<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soft.yozo.myrecode">

    <application
        android:name="com.soft.yozo.RecodeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/rainbow"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rainbow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity android:name=".activity.TestActivity"></activity>-->
        <activity android:name=".activity.AddFeeActivity" />
        <activity android:name=".activity.AddTipActivity" />
        <activity android:name=".activity.AddSubjectActivity" />
        <activity android:name=".activity.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>